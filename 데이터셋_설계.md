# 데이터셋 설계

## Task
- **Task 1**: `image → 초보자용 해설` 모델
- **Task 2**: 맥락 기반 해설용 context 저장

## 자막은 두 종류로 나뉜다
| 용도 | 필요한 자막 | 출처 |
|------|------------|------|
| 순간 해설 (Task 1 정답) | 타임코드 자막 (시간대로 잘라 이미지와 매칭) | 직접 추출 |
| 맥락 (Task 2, RAG) | 영상 전체 자막 | parquet |

순간 해설 정답은 "그 장면의 자막을 초보용으로 변환"해 만들기 때문에,
이미지와 자막을 시간대로 맞춰야 한다 → **타임코드 필요**.

## 합치는 키
`video_id` (+ 순간 해설은 `timestamp` / `start_time_sec` / `end_time_sec`)

## 산출물 (build_dataset.py)
- 입력: `lol_url_successed.jsonl` (1,122개)
- 이미지: `captures/{video_id}_{rank}.png` (영상당 top 5)
- 자막: 하이라이트 앞뒤 ±15초 윈도우
- CSV: `captures/highlights.csv`

## lol_subtitles.parquet
컬럼: video_id, url, title, highlight, transcription
transcription = 영상 전체 자막 통짜 (**타임코드 없음**) → Task 2 맥락용
