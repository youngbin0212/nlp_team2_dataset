# 데이터셋 정리

## 목표
LoL 하이라이트 영상에서 **게임화면 이미지**를 뽑는다.
나중에 두 가지에 쓴다:
- **이미지 → 초보자 해설** 모델 학습
- 맥락 기반 해설용 **context 저장**

## 자막은 두 종류로 나뉜다 (왜 시간대가 필요한가)
- **순간 해설**: 이미지 1장에 맞는 그 순간 자막 → **시간대(타임코드) 필요** → 내가 함
- **맥락**: 영상 전체 자막 → 팀원 parquet(`lol_subtitles.parquet`), 타임코드 없음
- 합칠 때 키 = **video_id** (+ 시간)
- ※ 지금은 자막 429(요청제한)라 **이미지만** 먼저, 자막은 나중에 따로

## 영상 두 종류 (총 1122개)
- **heatmap 있음 (804)** → 유튜브 '많이 본 구간'에서 뽑기 → `output/has_heatmap/`
- **heatmap 없음 (318)** → 영상 5등분해서 뽑기(앞 10초 skip) → `output/no_heatmap/`

## 이미지 거르기
- 영상당 **5장**, **게임화면만**
- 선수 얼굴·수상화면 제외 → 우하단 **미니맵** 있는지로 판별
- 이미지 이름: `영상ID_1~5.png`

## 결과물
```
output/
  has_heatmap/   이미지 + highlights.csv
  no_heatmap/    이미지 + highlights.csv
```

## 실행
```
python build_dataset.py
```
끊겨도 다시 실행하면 빠진 것만 이어서 함.

## 주요 파일
| 파일 | 설명 |
|---|---|
| build_dataset.py | 메인 (이미지 추출) |
| minimap_filter.py + minimap_ref.png | 게임화면 판별 |
| has_heatmap.jsonl / no_heatmap.jsonl | 영상 목록(2종류) |
| lol_subtitles.parquet | 팀원 전체 자막(맥락용) |
