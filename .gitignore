# 결과물 / 대용량 데이터 → Google Drive로 공유 (git 제외)
output/
captures/
transcripts/
*.parquet
*.jsonl
*.zip

# 로그인 쿠키 (민감정보 — 절대 커밋 금지)
*cookies*.txt

# 실행 로그 / 캐시 / 에디터
build_log.txt
build_err.txt
*_log.txt
*_err.txt
*.log
__pycache__/
.autocomplete.jsonl

# 안 쓰는 옛 코드
previous(dont_care)/
